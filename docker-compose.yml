version: "3.8"

services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - 8081
    environment:
      PORT: ${PORT:-8081}
      DB_URL: ${DB_URL}
      DB_MIGRATION_URL: ${DB_MIGRATION_URL}
    env_file:
      - .env
    networks:
      - dokploy-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.go-api.rule=Host(`api.stork-corp.xyz`)"
      - "traefik.http.routers.go-api.entrypoints=websecure"
      - "traefik.http.routers.go-api.tls.certResolver=letsencrypt"
      - "traefik.http.services.go-api.loadbalancer.server.port=8081"

networks:
  dokploy-network:
    external: true
